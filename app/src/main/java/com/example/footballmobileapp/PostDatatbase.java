package com.example.footballmobileapp;

/*abstract public class PostDatatbase extends RoomDatabase {

    private static PostDatatbase getInstance;

    public abstract mDb PostDao();

    public static synchronized PostDatatbase getPostDatabaseInstance(Context context) {
        if (getInstance == null) {
            getInstance = Room.databaseBuilder(context.getApplicationContext(), PostDatatbase.class, "note_database")
                    .fallbackToDestructiveMigration()
                    .build();
        }
        return getInstance;
    }
}*/
